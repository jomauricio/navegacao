import React from "react";
import { SafeAreaView, Text } from "react-native";

export default props => (
    <SafeAreaView style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center"
    }}>
        <Text style={{ fontSize: 46 }}>APP</Text>
    </SafeAreaView>
)